import { html } from '~utils';

export const data = {
  layout: 'base',
  title: 'tv',
  description: 'My Favorite Series',
  tags: ['nav'],
  order: 4,
};

export const render = ({}) => {
  return html``;
};
